import PolicyPage from "@/components/PolicyPage";
export default function ReturnsPage() { return <PolicyPage slug="return-policy" title="Returns & support" description="Know what to expect if something isn’t quite right with your order." />; }
